import React from "react"
import { Wrap } from "./components/commons"
import { gstyle } from "./components/style"

export default function ({ navigation, children }: { navigation?:any, children: any }) {
    return (
        <Wrap style={gstyle.body}>
            
        </Wrap>
    )
}